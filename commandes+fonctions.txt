https://github.com/andfanilo/pyspark-tutorial/tree/master

cd C:\Users\avial3\Documents\pyspark-tutorial-master

conda activate pyspark-tutorial-j
pip install -r requirements.txt
set PYSPARK_PYTHON=python

jupyter lab

fonction is_unique :
def is_unique(numbers):
    """
    Return True if list of numbers contains only unique numbers, False otherwise.
    """
    # Here, I will help a bit, use a dictionary to store encountered numbers.
    encountered = {}
    not_unique=0
    
    for i in numbers :
        encountered[i]=numbers.count(i)
        
    for i in encountered :
        if encountered[i]>1 :
            not_unique+=1
    if not_unique==0 :
        return True
    else : 
        return False
        
    raise NotImplementedError()

# Apply the operation to the RDD
result_rdd = matrix_rdd.map(op1)